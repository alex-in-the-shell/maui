<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ShoppingListClient.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="10"
            Padding="15,0">

            <Entry x:Name="NewItemName" 
                   Placeholder="Enter item name" 
                   HorizontalOptions="Fill"/>

            <Entry x:Name="NewItemDescription" 
                   Placeholder="Enter description" 
                   HorizontalOptions="Fill"/>

            <Button Text="Add Item"
                    Clicked="OnAddItemClicked"
                    HorizontalOptions="Center"/>

            <CollectionView x:Name="ItemsList"
                           ItemsSource="{Binding ShoppingItems}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Label Text="{Binding Name}"
                                   Grid.Column="0"
                                   FontSize="16"/>

                            <Switch IsToggled="{Binding IsCompleted}"
                                    Grid.Column="1"
                                    Toggled="OnItemToggled"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
